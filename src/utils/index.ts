import { Utils } from '../schemas/interfaces'
import * as attribute from './attribute'
import * as number from './number'
import * as router from './router'
import * as api from './api'
import * as gsap from './gsap'

const utils: Utils = {
  attribute,
  number,
  router,
  api,
  gsap
}

export default utils
